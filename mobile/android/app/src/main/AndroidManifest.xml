<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Supabase API calls -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- GPS check-in/out at stores -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- The workday trail, recorded while a day is open.

         Background location is a separate grant from the two above and cannot be
         obtained from an in-app prompt on Android 11+ — the rep has to choose
         "Allow all the time" in system settings. Without it the app still works
         and still records a trail; it just stops whenever the app is not on
         screen, which is what `LocationTrackingMode.foregroundOnly` reports.

         FOREGROUND_SERVICE_LOCATION is the typed permission Android 14 requires
         alongside the untyped one. geolocator declares the service itself, with
         android:foregroundServiceType="location", so there is no <service> here.

         WAKE_LOCK backs ForegroundNotificationConfig.enableWakeLock: without it
         the device sleeps between fixes and Android delivers the whole batch on
         waking, which reads as a rep who sat still for hours and then teleported. -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <application
        android:label="Gold Fortune"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
